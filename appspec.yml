version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html
hook:
  ApplicationStop:
    - location: scripts/app_stop.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/app_start.sh
      timeout: 300
      runas: root